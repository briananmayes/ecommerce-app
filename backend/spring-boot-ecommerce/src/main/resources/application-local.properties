############# Database Properties for PostgreSQL ##################
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/fullstack-ecommerce?currentSchema=full-stack-ecommerce
spring.datasource.username=brianamayes
spring.datasource.password=
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


############# Database Properties for mySQL ##################
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.datasource.url=jdbc:mysql://localhost:3306/full-stack-ecommerce?useSSL=false&useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=UTC
# spring.datasource.username=ecommerceapp
# spring.datasource.password=ecommerceapp
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

server.port=8443

#####
#
# HTTPS configuration
#
#####
# Enable HTTPS support (only accept HTTPS requests)
# server.ssl.enabled=true

# Alias that identifies the key in the key store
# server.ssl.key-alias=luv2code

# Keystore location
# server.ssl.key-store=classpath:luv2code-keystore.p12

# Keystore password
# server.ssl.key-store-password=secret

# Keystore format
# server.ssl.key-store-type=PKCS12